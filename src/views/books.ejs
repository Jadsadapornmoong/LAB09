<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Book Management</title>
    <link rel="stylesheet" href="/styles.css" />
</head>
<body>
    <div class="container">
        <h1><%= searchQuery ? `Search Results for "${searchQuery}"` : 'Book Management' %></h1>
        
        <div style="margin-bottom: 20px; text-align: center;">
            <a href="/" style="color: #a18cd1; text-decoration: none; font-weight: bold;">&larr; Back to Home</a>
        </div>

        <form action="/books" method="POST">
            <input type="text" name="bookName" placeholder="Book name" required>
            <button type="submit">Add</button>
        </form>

        <h2>Book List</h2>
        <div id="book-list">
            <% if (books.length === 0) { %>
                <p style="text-align: center; color: #888;">No books found.</p>
            <% } else { %>
                <% books.forEach(function(book) { %>
                    <div class="book-item">
                        <span>
                            <strong>#<%= book.bookNo %></strong> <%= book.bookName %>
                        </span>
                        <form action="/books/delete/<%= book.bookNo %>" method="POST" style="margin: 0;">
                            <button type="submit" class="btn-delete" onclick="return confirm('Are you sure you want to delete this book?')">Delete</button>
                        </form>
                    </div>
                <% }); %>
            <% } %>
        </div>
    </div>
</body>
</html>